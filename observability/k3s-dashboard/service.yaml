apiVersion: v1
kind: Service
metadata:
  name: k3s-dashboard
  labels:
    app: k3s-dashboard
spec:
  type: NodePort
  selector:
    app: k3s-dashboard
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
